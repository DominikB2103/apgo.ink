# Documents folder

Each long-form HTML document should live in its own folder:

```text
documents/
  my-new-note/
    index.html
```

Use `_template/` as the starting point. After creating a new folder, add its title, section, date, tags, URL, and preview text to the `archiveItems` array in `assets/js/site.js`.
